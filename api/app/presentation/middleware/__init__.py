"""Presentation layer middleware components.

Middleware:
  SecurityHeadersMiddleware — adds security headers; removes Server header
  RateLimiter               — in-memory sliding-window login rate limiter
"""
