"""FastAPI route handlers for the Delivery Tracking API.

Routers:
  auth_router       — POST /api/auth/login, /refresh, /logout
  deliveries_router — GET /api/deliveries, /api/deliveries/{id}
  system_router     — GET /api/health, /api/carriers
"""
